@charset "UTF -8";
:root {
    --fonte1: Verdana, Geneva, Tahoma, sans-serif;
  }

*{
    margin: opx;
    padding: 0px;
}

html, body {
    font-family: var(--fonte1);
    min-height: 100vh;
    background-color: darkgray;

}

